<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ADAPT" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/adapt_primary</item>
        <item name="colorSecondary">@color/adapt_accent</item>
        <item name="colorSurface">@color/adapt_surface</item>
        <item name="android:fontFamily">sans</item>
        <item name="android:navigationBarColor">@color/adapt_surface</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:forceDarkAllowed">false</item>
        <item name="bottomSheetDialogTheme">@style/ThemeOverlay.ADAPT.BottomSheet</item>
    </style>

    <style name="Theme.ADAPT" parent="Base.Theme.ADAPT" />

    <style name="ThemeOverlay.ADAPT.BottomSheet" parent="ThemeOverlay.Material3.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/Widget.ADAPT.BottomSheet</item>
    </style>

    <style name="Widget.ADAPT.BottomSheet" parent="Widget.Material3.BottomSheet.Modal">
        <item name="shapeAppearanceOverlay">@style/ShapeAppearance.ADAPT.BottomSheet</item>
        <item name="backgroundTint">@color/adapt_surface</item>
    </style>

    <style name="ShapeAppearance.ADAPT.BottomSheet" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">24dp</item>
        <item name="cornerSizeTopRight">24dp</item>
    </style>
</resources>
