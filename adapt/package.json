{
  "name": "adapt-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Workspace helper scripts for ADAPT services",
  "engines": {
    "node": ">=20",
    "npm": ">=10"
  },
  "scripts": {
    "install:all": "npm --prefix backend ci && npm --prefix engine-service ci && npm --prefix frontend ci",
    "build:all": "npm --prefix backend run build && npm --prefix engine-service run build && npm --prefix frontend run build",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "single-image:up": "docker compose --profile single-image up -d --build",
    "single-image:down": "docker compose --profile single-image down"
  }
}
